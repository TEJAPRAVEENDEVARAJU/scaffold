fastapi
uvicorn[standard]
httpx
pydantic
playwright    # optional for browser automation - or use playwright-python
jinja2
pandas
python-dotenv
loguru
# ADK-Python package placeholder (actual ADK name / install method as provided by course)
# For demo: use `openai` or `google-generativeai` if available, else stub
